namespace OmegaAssetStudio.WinUI.Services
{
    /// <summary>
    /// Nothing, and here for the sake of a line in a file that is not to be
    /// touched.
    /// </summary>
    /// <remarks>
    /// CrossUpkSiblingDiscovery names this namespace at its head and uses
    /// nothing from it. In the tool it comes from the namespace exists, so the
    /// line costs nothing there; here it would not compile.
    /// <para>
    /// The line could be deleted instead. It is not, because the whole point of
    /// keeping those files as they stand is that no one - including whoever
    /// reads this next - gets to decide which of their lines are unnecessary.
    /// A file that is byte-for-byte the original can be checked against the
    /// original. One that is nearly the original cannot.
    /// </para>
    /// </remarks>
    internal static class NamespacesTheCopiedFilesName
    {
    }
}
