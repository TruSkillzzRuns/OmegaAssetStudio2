<UserControl
    x:Class="OmegaAssetStudio2.App.Controls.Skeleton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    SizeChanged="OnSizeChanged">

    <!-- Gray base rounded rect + a soft-light gradient that animates across.
         Clipped via the Grid Clip property so the shimmer doesn't leak. -->
    <Grid x:Name="RootClipGrid"
          Background="{ThemeResource OmegaAssetStudio.PanelSecondaryBrush}">
        <Rectangle x:Name="ShimmerBar"
                   Width="160"
                   HorizontalAlignment="Left"
                   IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="#00FFFFFF" Offset="0.0" />
                    <GradientStop Color="#22FFFFFF" Offset="0.5" />
                    <GradientStop Color="#00FFFFFF" Offset="1.0" />
                </LinearGradientBrush>
            </Rectangle.Fill>
            <Rectangle.RenderTransform>
                <TranslateTransform x:Name="ShimmerTranslate" X="-200" />
            </Rectangle.RenderTransform>
        </Rectangle>
    </Grid>
</UserControl>
